# Night build — LIVING STATE (read this FIRST to resume)

**Updated:** 2026-06-25, session start.
**Branch:** `phase/9c-roster` (tip `4bf2762`, clean). Stack: `main ← #37 ← #38 ← #39(=9c)`.
**Plan:** `./PLAN.md`. Standing decisions there (no merge to main; draft PR chain).

## Status board
`TODO` · `DOING` · `DONE(verdict)` · `BLOCKED`

- **U1** split `dispatch.ts` — **DONE** (CI fix `aef2ea3`). Checker PASS on `c19dcca`; CI build-test now GREEN (run 28139601145, all steps ✓) + Sonar exit 0 (PR#39: 0 issues/0 hotspots). NOTE: HTTPS push needs one-shot `git -c credential.helper= -c credential.helper='!gh auth git-credential' push` — bare `git push` hangs on the `store` helper.
- **U2** PR stack readiness report — TODO
- **U3** 0d pre-flight intake-audit — **DONE** → `docs/intake/2026-06-25-0d/preflight-pack.md` (4 agents, tree-verified). See "0d build notes" below.
- **U4** 0d build (`phase/9d-arsenal`) — **4a DONE + 4b DONE** (Checker-pending for 4b). Commits: `b…` RRF (`feat(skills): deterministic RRF fusion`), `098ddb7` select-fusion, `1262cc5` dispatch-thread, `1b68944` S7735 fix. Gate 5/5 green (test all pkgs · lint · build · smoke 32/32), CI build-test PASS (run 28140143845), Sonar exit 0 (PR#40: 0 issues/0 hotspots) + gate PASS. PR **#40 DRAFT** (base `phase/9c-roster`). Source (b) arsenal retriever RRF-fused into `selectLibrarySkills` (optional `retriever?`, omitted=byte-identical, throws=fallback, `degraded`=LLM-rank-absence only — regression trap #1 held: `dispatch-arsenal.test.ts:69` green); `arsenalRetrieverFor()` singleton (QMD_ARSENAL, never per-task) threaded into `planMission`. skills 36/36.
  - **4b DONE** (gate+CI+Sonar GREEN). Commits: `4932095` (`feat(agents): 0d/4b cold-agent suggestion event (§5 data-only)`) + `f51c22b` (`test(agents): build agent index in beforeAll (CI: gitignored artifact)`). New `packages/agents/src/cold-agent-suggest.ts` — pure deterministic scorer (`scoreColdAgentSuggestion`, role+domains+name token overlap, ties by id asc, zero-LLM, margin=2 vs planner hint). Wired into `planMission`: load agent L1 index ONCE/mission via `loadAgentLibrarySafely()` (try/catch→[] so the Next bundler's `import.meta.url` non-file-URL throw degrades to no-suggestions, never crashes planning), score each task, emit ONE `cold_agent_suggested` event (`logEvent`, no agentId — FK-safe, payload `{taskId,suggestedAgentId,score,reason}`). **§5 HARD RULE held: data only** — `t.agentHint`/`tasks.agentId` persisted unchanged, NOT inserted into `TIER_B_DELEGATION_MAP`, no delegation routing; integration test asserts all three + that the suggested id is absent from the map. Real-library verify: t6[reviewer]→`database-reviewer`(4), t4→`language-reviewer`(4), t5→`pr-test-analyzer`(4), t2→(none). **Gotcha solved:** agents `library/index.json` is a gitignored build artifact (CLAUDE.md §3) → absent in CI → empty library → 0 suggestions; fixed by `buildAgentLibraryIndex(REPO_ROOT)` in test `beforeAll` (mirrors `agent-library-index.test.ts`). agents 130/130 (+4). Gate 5/5 green, CI build-test PASS (run 28140875929), Sonar exit 0 (PR#40 still 0/0) + gate OK.
  - **4c DONE** (gate+CI+Sonar GREEN). Commit `35caee3` (`feat(core): 0d/4c opt-in QMD MCP for in-loop agents (least-privilege)`). `ClaudeCodeLLMOptions.mcp?: boolean` opt-in (default OFF): when ON, the SDK `query()` options carry `mcpServers: { qmd: { command:'qmd', args:['mcp'] } }` + scoped `allowedTools: ['mcp__qmd__query']` — LEAST PRIVILEGE (NOT get/multi_get/status). When OFF (default), options are **byte-identical** to today (spread `...(opts.mcp ? {...} : {})` adds zero keys). Two new `llm.real.test.ts` cases (mock SDK `query`, inspect captured options): mcp:true ⇒ mcpServers.qmd + allowedTools∋mcp__qmd__query & ∌get/status; mcp off ⇒ no `mcpServers`/`allowedTools` keys at all. **Gotcha:** `as const` made the literals `readonly` → SDK `McpStdioServerConfig.args:string[]` is mutable → tsc fail in apps/web build+lint; fixed with `qmdMcpServers()`/`qmdAllowedTools()` factory fns returning mutable types. No long-lived MCP server in worker hot-path — opt-in only, QMD still queried out-of-worker via CLI. Default path untouched: core 109/109 (was 107, +2), full `pnpm -r test` green, no regression. Gate 5/5 (test·lint·build·smoke 32/32), CI build-test PASS (run 28141281550, all steps ✓), Sonar exit 0 (PR#40: 0 issues/0 hotspots) + gate OK.
  - **4d DONE** (gate+CI+Sonar GREEN). Commits: `0b4cc91` (`feat(memory): 0d/4d golden-set minScore floor`) + `d51d121` (`feat(memory): 0d/4d add 5 arsenal golden rows (qmdOnly)`) + `f52d1dc` (`ci: 0d/4d arsenal-change eval trigger`). **(1) minScore floor (TDD):** optional `minScore?: number` on `GoldenQuery`; in `runRetrievalEval` a row passes only if a *matching* top-k hit also clears the floor (silent rank-collapse guard); omitted ⇒ legacy. RED→GREEN `eval.test.ts` case (above-floor passes, below-floor=999 fails). **(2) 5 arsenal rows** appended to `golden-queries.json`, all `qmdOnly:true` (auto-skip on FTS) — `arsenal-defensive-agent`(minScore 0.3, resolves to AGENT doc `security-defensive-specialist` = §3c cold-agent assertion), `arsenal-prod-audit`, `arsenal-db-reviewer`, `arsenal-security-scan`, `arsenal-phishing`. Verified live both ways: **`MAS_RETRIEVAL_BACKEND=fts pnpm mem:eval` → 11 skip, exit 0** (honest offline); **`MAS_RETRIEVAL_BACKEND=qmd pnpm mem:eval` → 11 pass, 0 fail** (all new rows genuinely recall). 11 = 3 semantic + 8 arsenal. **(3) CI arsenal-trigger:** new `arsenal-eval` job in `ci.yml`, gated by a `changes` detector job (`dorny/paths-filter@de90cc6` SHA-pinned = v3.0.2 verified, supply-chain convention) on the exact `arsenalSources()` globs (`packages/skills/library/**`, `packages/agents/library/**`, `.claude/agents/**`, `docs/rules/**`, `.claude/commands/**`, + `golden-queries.json`); runs `pnpm arsenal:build && pnpm mem:eval`. **HONEST-SKIP decision (build-note #5, commented loudly in the workflow):** CI is Node 20 and CANNOT host QMD (needs Node≥22 + 4.4GB models), so `MAS_RETRIEVAL_BACKEND: fts` is PINNED → every `qmdOnly` row auto-skips and the step is green WITHOUT faking semantic recall; what it *does* prove on CI = `arsenal:build` regenerates the index without crashing + `mem:eval` parses/replays the gold set end-to-end (catches corrupt gold set / broken harness). The always-on recall gate stays `eval.test.ts` (FTS fixture) in `pnpm -r test`; live QMD recall asserted locally before each arsenal change. Gate 5/5 green (core 109 · db 15 · skills 36 · memory 88(+1) · agents 130 · web 143 · worker 8 · lint · build · smoke 32/32). CI run **28141672410 = all 3 jobs success** (build-test ✓, changes ✓ detected golden-queries.json change, arsenal-eval ✓ FTS-honest 11-skip). Sonar exit 0 (PR#40: 0 issues/0 hotspots) + gate **OK**.
  - **0d (4a–4d) ALL BUILT** — pending a **wave-level Checker** (4a + 4b individually PASS; 4c + 4d not yet Checker-reviewed). PR **#40 DRAFT** (base `phase/9c-roster`), HEAD `f52d1dc`.
  - **(prev) DOING (4a in progress, NOT done)**: Branch `phase/9d-arsenal` created off clean 9c tip `88279f6` (descendant doc-commit of CI fix `aef2ea3`; `aef2ea3` itself is not the 9c tip — 9c tip is `88279f6`, clean). Done so far: `packages/skills/src/rrf.ts` (deterministic `rrfFuse`, c=60, ties by id asc) + `rrf.test.ts` (GREEN expected, not yet run); started `select.test.ts` edit (imports `ArsenalRetriever` — type does NOT exist yet → test file currently RED on import). **REMAINING for 4a:** (1) add `export interface ArsenalRetriever { query(q, opts?): {id;score}[] }` + optional `retriever?` to `SelectParams` in `select.ts`; (2) wire source-(b): `retriever.query(taskText,{collection:'mas-arsenal',limit:k})` in try/catch→[], map hit ids→SkillMeta via router (drop unknown/out-of-scope), RRF-fuse with source-(a) tag-score ids, slice top-K shortlist; **MUST keep `degraded` = LLM-rank-absence only** (retriever present + no llm ⇒ still `degraded:true`); omitted ⇒ byte-identical; throws ⇒ fallback to (a). (3) append select.test cases: stub-retriever surfaces a tag-missed skill / omitted=byte-identical / throws=fallback+degraded-unchanged. (4) `arsenalRetrieverFor()` in `mission-llm.ts` (collection `QMD_ARSENAL`, try/catch→undefined, `_memRetriever`-style singleton, NEVER per-task) + thread into every `selectLibrarySkills` call in `planMission` loop (`dispatch.ts:92`), built ONCE before the loop. (5) dispatch test: one arsenal retriever built/reused per mission. Then full 5-check gate + commit + push + draft PR + CI + Sonar. KEY FACTS verified: `MemoryHit` has `{id,score,...}` (structurally compatible — do NOT import @mas/memory into packages/skills); `@mas/memory` exports `QMD_ARSENAL`,`createRetriever`,`MemoryRetriever`,`RetrievalBackend`; `dispatch-arsenal.test.ts:69` asserts `degraded===true`&`skills===skillsJson` under no-llm (regression trap #1). NOT committed yet.
- **U5** self-audit §13 + Sonar debt — TODO
- **U6+** 0e / Étape 1 — TODO

## 0d build notes — from pre-flight pack. READ before U4. (`docs/intake/2026-06-25-0d/preflight-pack.md`)
1. `selectLibrarySkills` `degraded` flag = **LLM-rank absence, NOT retriever presence**. `dispatch-arsenal.test.ts:69` asserts `degraded===true` & `skills===skillsJson` under no-`llm`. The semantic union must **not** flip `degraded`. #1 regression trap.
2. `QmdRetriever.query` = **blocking 30s `execFileSync`** (`retriever.ts:287`). NEVER call in the per-task `planMission` loop. Do **one cached arsenal query per mission** via a new `arsenalRetrieverFor()` in `mission-llm.ts`.
3. `mas-arsenal` is **excluded** from `QMD_MEMORY_COLLECTIONS` (`retriever.ts:233`). 4a must query `QMD_ARSENAL` explicitly.
4. 4c: runtime SDK path has **zero MCP wiring** (`llm.real.ts:73`, no `mcpServers`/`allowedTools`). Add opt-in least-privilege (`mcp__qmd__query` only) so the default path stays byte-identical.
5. CI gap: `pnpm mem:eval` not in `ci.yml`; CI=Node20 but `qmd:setup` needs Node≥22 + 4.4GB models. Keep new golden rows `qmdOnly` → auto-skip offline (explicit decision, **not** a silent skip).
6. KILL path 4a: if deterministic RRF breaks tests, ship `union+dedupe+stable-sort` (defer RRF→0e). The deterministic floor (no crash, no empty shortlist) is non-negotiable.

## Log (newest last)
- **session start** — Recon done. Confirmed: `dispatch.ts`=1026L (>800 debt real);
  PR stack #37/#38/#39 all DRAFT + MERGEABLE, linear; 0d scope = "exploitation de
  l'arsenal" (ROADMAP 433-442), edits `dispatch.ts:274-278` → U1 must precede it;
  QMD MCP live (1132 docs, collections mas-arsenal/memory/workflows/knowledge).
  Wrote PLAN.md + this file. Launched U1 refactor sub-agent (background).
- **U3 DONE** — preflight-0d workflow returned (4 agents, 324k subagent tokens, ~4min).
  Pack tree-verified; 6 gotchas captured above. 0d build is now de-risked & specced.
- **U1 verified by me** — 5 local checks green, committed+pushed `c19dcca`, backlog RESOLVED.
- **U1 Checker = PASS** (high conf, adversarial diff vs `c19dcca^`) → `U1-checker-verdict.md`. 7/7 contracts hold.
- **U1 SONAR gate passed** (PR#39: 0 issues/hotspots/dup) BUT ⚠️ **CI check "build-test" FAILED on #39** — discovered after I prematurely marked DONE. Sonar ≠ all-green. My 5 LOCAL checks passed (test/lint/build/smoke), so CI failure is env-specific — **prime suspect: the pre-existing `TS2532` strict-null in `*.test.ts` if CI's build-test runs a stricter `tsc` including tests** (delegate.test.ts, dispatch-chaining/delegate.test.ts, delegate.with-diff.test.ts). **U1 = NOT DONE.**
- *FIRST on resume*: `gh pr checks 39 --repo nyvlemyt/MultiAgentOS` + `gh run view <build-test-run-id> --log-failed`. Confirm whether failure is the TS2532 (pre-existing, but if CI gates on it, fix the 4 test files with precise narrowing) or a real regression from the split. Fix on `phase/9c-roster`, re-push (use the gh-credential-helper push form above), re-confirm CI build-test green. ONLY THEN U1 DONE → branch 9d → U4.
- *next (fresh session)*: branch `phase/9d-arsenal` off clean 9c (`c19dcca`) → build **U4 (0d)** per `docs/intake/2026-06-25-0d/preflight-pack.md` §4 (4a→4b→4c→4d, TDD + 5 checks each). Then U5 self-audit + TS2532 debt. Status board above: set U1→DONE.
  When Sonar exit 0 → U1 **DONE** → branch `phase/9d-arsenal` off clean 9c tip → build U4 (0d) per pre-flight pack.
  If Sonar flags issues → fix on 9c first, re-push, re-check, THEN branch.
- **U4/4a Checker = PASS → 4a fully DONE** (adversarial review of `phase/9c-roster..phase/9d-arsenal`, commits `84ea586`/`098ddb7`/`1262cc5`/`1b68944`) → `U4a-checker-verdict.md`. All 7 contracts hold: (1) `degraded`=LLM-rank-absence only, retriever never flips it (`select.ts:203-211`), `dispatch-arsenal.test.ts:69` green & intent-unmodified; (2) omitted ⇒ byte-identical (RRF fires only when `semanticOrder.length>0`); (3) throwing ⇒ try/catch→`[]`→tag-only fallback, no propagation; (4) `rrfFuse` pure deterministic `Σ1/(60+rank)`, dedupe by id, ties by id asc, zero LLM; (5) `arsenalRetrieverFor()` singleton (`_arsenalBuilt` guard), built ONCE before the task loop (`dispatch.ts:73`), reused per task — never the 30s per-task spawn; (6) `ArsenalRetriever` local interface, **no `@mas/memory` import** in `packages/skills/src`; (7) tests genuinely cover — tag-missed `sec-19` enters shortlist via semantic source. No §5 leakage, no 4b/4c/4d scope-creep, no dead code. Local: skills 17/17 + dispatch-arsenal 2/2 green. **4a is fully DONE. REMAINING: 4b/4c/4d (not started).**
- **U1 CI FIXED → DONE** (`aef2ea3`) — Root cause was **NOT** the suspected TS2532 (lint+typecheck passed in CI). The build-test failure was a **Playwright smoke-test flake**: 3 routes (`/missions/mission_seed_001`, `/skills`, lang-pill `/`) timed out on `page.goto` (cold `pnpm dev` first-compile under 2-worker CI load); 29/32 passed and the two prior runs on this SHA family were green → timing, not regression. The dispatch.ts split touched only `packages/agents` (no web route). Fix (config-only, no scope-creep, no tsconfig change): `apps/web/playwright.config.ts` test timeout 30s→60s + `navigationTimeout: 45s` + `retries: 1` on CI. Verified locally (smoke 32/32 with CI=1, full unit suite green incl. agents 125/125), pushed, CI run **28139601145 = build-test PASS (all steps ✓)**, Sonar exit 0. **U1 DONE.** Branch `phase/9d-arsenal` off tip `aef2ea3` (NOT `c19dcca` — take the CI fix) for U4.
- **U4/4b Checker = PASS → 4b fully DONE** (adversarial review of `1262cc5..phase/9d-arsenal -- packages/agents`, commits `4932095` feature + `f51c22b` CI fix) → `U4b-checker-verdict.md`. **§5 DATA-ONLY holds end to end:** `tasks.agentId` persists `t.agentHint` in the `db.insert` (`dispatch.ts:142`) *before* the suggestion block; the suggestion (`dispatch.ts:168-188`) is a lone `logEvent` — never UPDATEs tasks, never re-assigns `t.agentHint`, never writes `TIER_B_DELEGATION_MAP`, never routes delegation. `scoreColdAgentSuggestion` (`cold-agent-suggest.ts`) is a pure value-object scorer (token overlap, ties by id asc, no Date/Math.random/QMD/network). Test asserts all 3 non-mutations + delegation-map absence (`cold-agent-suggest.test.ts:92-101`), not just that an event fired. Event = exactly one `cold_agent_suggested`/qualifying task, payload `{taskId,suggestedAgentId,score,reason}`, no event when hint wins (margin gate; t2 no-event asserted). Graceful degradation: `loadAgentLibrarySafely()` try/catch→`[]` + `loadAgentLibraryIndex` returns `[]` when index absent + scorer `library.length===0` short-circuit ⇒ missing index = 0 suggestions, never a throw (f51c22b adds test-side `beforeAll(buildAgentLibraryIndex)` for the gitignored artifact — runtime path untouched). No 4c/4d scope-creep, no dead code, no dispatch behaviour change. Local: cold-agent-suggest 4/4 + dispatch* 26/26 green. **4b fully DONE. REMAINING: 4c/4d (not started).**
