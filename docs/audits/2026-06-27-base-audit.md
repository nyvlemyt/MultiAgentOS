# Base Audit — Living Knowledge OS (Round 1, read-only)
- Date: 2026-06-27
- Method: `docs/workflows/intake-audit-template.md` applied per pillar; confront base vs `docs/knowledge/` + state-of-art. Fanned out one read-only agent per pillar; orchestrator merged into this file (design spec §8 / Round-1 plan Task 1 Step 3).
- Feeds: ADR 0008 (decisions), `docs/STRUCTURE.md` (conventions), design spec §9 Round-2 backlog (cards).
- Scope: read-only constat. No remediation is performed here; each finding is routed to its destination (charte / ADR 0008 / a named Round-2 backlog card). The findings cite `path:line` — they are facts about the code/docs at HEAD, not opinions.

## Pillar 1 — Memory (5-register, capture, consolidation lifecycle)
| Constat | Gravité | Remédiation |
|---|---|---|
| **No consolidation / dedup / decay anywhere in the memory layer.** A repo-wide search for `dedup\|consolidat\|merge\|prune\|evict\|TTL\|expire\|decay\|already.proposed` returns zero hits in `packages/memory/src/` and the worker. `promoteCandidate` (registers.ts:283) appends unconditionally; `runCloseOutRitual` (auto-capture.ts:22) re-fires per mission with no cross-mission dedup. The doctrine's "memweave" arch (memory-patterns.md:280) and the `state.json` dedup pattern (memory-patterns.md:255) are both unimplemented — registers grow append-only forever. | high | → Round-2 backlog card: "memory-consolidation-and-dedup" |
| **No `capture_failed` lifecycle state — capture failures are invisible to the inbox.** The `status` enum is `['pending','accepted','rejected']` (schema.ts:229), so a capture that throws mid-flight cannot be recorded as a failed candidate. `captureCandidates` (capture.ts:33) does a single bulk `db.insert` with no try/catch; `runCloseOutRitual` (auto-capture.ts) is guarded only by the replay event, so a partial failure silently drops candidates with no audit trail. The lifecycle contract requires capture to be observable (continuous-learning-and-memory-lifecycle.md:39). | medium | → ADR 0008 |
| **No per-candidate trust/provenance signal — auto-file trust is a coarse path-prefix allowlist.** `memory_candidates` has no `trust` or `source_key` column (schema.ts:224-236); auto-file trust is decided in `intake-gate.ts:54` by `String.startsWith` matching against `config/intake.trust.json#trusted_sources`. The continuous-learning spiral's stage-3 "instinct scoring + persistence" gate (continuous-learning-and-memory-lifecycle.md:36) has no column to record a score against, so promotion cannot be gated by trust/signal density as the doctrine requires. | medium | → Round-2 backlog card: "memory-candidate-trust-and-scoring" |
| **Runtime capture covers only 3 of the 5 registers; `evals` + `learnings` get no automated feed.** `runCloseOutRitual` emits exclusively `type: 'project'` bodies (auto-capture.ts:46,57,62) whose text matches only the `mission-summary`/`kw-decision`/`kw-blocker` classifier rules (classifier.ts:40-46) → journal / decisions / blockers. Nothing at runtime produces an `evals` (EVAL-XXX) or `learnings` (LRN-XXX) candidate; `eval.ts` is a retrieval-eval harness, not a register writer. The doctrine mandates all 5 registers as live capture targets (project-doctrine.md:20-26, 116-124). | medium | → Round-2 backlog card: "runtime-capture-all-five-registers" |
| **SessionStart / PreCompact / SessionEnd lifecycle wiring is absent — capture is mission-end only.** continuous-learning-and-memory-lifecycle.md:6-23 specifies a hook contract (bounded SessionStart inject, PreCompact/SessionEnd flush to `MemoryProposal`) and calls it "the missing piece" (line 23). The only capture chokepoint is `runCloseOutRitual` at mission end; mid-session learnings are lost before the ritual fires. | low | → charte (STRUCTURE.md) |

**High/blocking in this pillar:** the no-consolidation / no-dedup / no-decay finding (registers grow append-only forever, "memory-consolidation-and-dedup").

## Pillar 2 — Task decomposition (mas-mission-planner DAG)
| Constat | Gravité | Remédiation |
|---|---|---|
| The only production planner, `planMission`, calls `mockMissionPlanner` (`packages/agents/src/dispatch.ts:239`), which returns a hardcoded, mission-agnostic 6-task DAG built around an "empty-state UI" example (`packages/core/src/llm.ts:131-220`). The user's natural-language `objective`/`title` is passed in but only echoed back (`llm.ts:215`) and pattern-sniffed for one `/security\|cyber/` keyword branch (`llm.ts:198-210`) — it never drives an LLM decomposition. There is **no** `realMissionPlanner` anywhere (grep: zero hits). The skill body (clarify→decompose→assign→estimate, CoT, signal-density) describes a planner **not wired into runtime**. | blocking | → Round-2 backlog card: "wire-real-mission-planner" (replace `mockMissionPlanner` in `planMission` with an LLM-backed planner driven by the mas-mission-planner SKILL) |
| `clarifyingQuestions` exists in the `PlannerOutput` type (`packages/core/src/llm.ts:124`) and is the skill's headline "ask-before-plan" contract (SKILL.md:52, fiche:63), but `mockMissionPlanner` hardcodes it to `[]` (`llm.ts:214`) and **no caller ever reads it** (grep: only test fixtures + the mock). The "clarify-before-plan / escalate-rather-than-invent" practice (agent-patterns.md:57) is documented but inert — missions can never pause for clarification. | high | → Round-2 backlog card: "clarifying-questions-gate" (planMission halts at `status:clarified` and surfaces `clarifyingQuestions` before decomposition) |
| Both the skill (SKILL.md:109) and the fiche (mission-planner.md:101,109) declare "DAG is acyclic / no cycle" a **binary verification criterion**, and `selectRunnableTasks` (`dispatch.ts:319-325`) silently assumes acyclicity when resolving `dependsOn` — yet there is **no** cycle/topo validation in `packages/agents` or `packages/core` (grep `cycle\|acyclic\|topolog`: zero in planner path). A cyclic plan from a future real planner would deadlock dispatch with no diagnostic. Safe today only because the mock DAG is a fixed chain. | medium | → Round-2 backlog card: "planner-acyclicity-validation" |
| Schema field-name drift across the three planner artifacts: live code + skill use `estimatedQuotaUnits` (`llm.ts:128,218`; SKILL.md:82), but the fiche uses `estimatedCostCents` (`mission-planner.md:35,66,89,93,104`). A real planner authored against the fiche would emit a field the `PlannerOutput` type rejects. Same single-source-of-truth violation flagged in the .md-excellence backlog (quota-window doc↔schema drift). | medium | → charte (STRUCTURE.md): name the canonical planner-output schema (one field) + reconcile fiche to `estimatedQuotaUnits` |
| Risk tagging itself is real and sound — `classifyRisk` (`packages/core/src/risk-classifier.ts:57-81`) enforces the §5 always-gate rule table + perms `high/blocking` categories, wired via `resolveTaskRisk`→`classifyAndPersistTask` (`dispatch.ts:99-213`). But it classifies only the **mock's hardcoded task text**, never the real mission. The gating engine is correct yet starved of genuine input until the real planner lands. | medium | → ADR 0008 (record that risk-gating correctness is contingent on the planner emitting real per-task descriptions — couples to the mock-planner finding) |

**High/blocking in this pillar:** **blocking** — the production planner is a hardcoded mock; natural-language missions are not actually decomposed. **high** — `clarifyingQuestions` / clarify-before-plan is documented doctrine but completely inert.

## Pillar 3 — Agent creation (Tier A fiches, ≤7 tools)
| Constat | Gravité | Remédiation |
|---|---|---|
| The **≤7-tools-per-agent rule** (agent-patterns.md:198 + CLAUDE.md §12 "Apply ≤7 tools per agent rule") is **absent from the canonical fiche schema** (`AGENTS.md` §2, lines 13-58 — no `tools`/skill-cap key) and **enforced nowhere in code**: `registry.ts:36-52` `validateFiche()` checks key presence only, never counts `favorite_skills`+`required_skills`; `fiche-quality.test.ts` has no count assertion. A future fiche could ship 12 skills and pass every gate. (Live fiches are all fine — max 4, on `architect.md:21`.) | medium | → ADR 0008 (record the rule + where it binds) + Round-2 backlog card: "fiche-skill-cap-enforcement" (add ≤7-skill cap to `validateFiche()` + AGENTS.md §2 schema) |
| **Roster divergence: 10 fiches vs 9 in seed & fixtures.** `packages/agents/fiches/` ships 10 (incl. `architect`), and `AGENTS.md` §3 says "shipped fiches (10)". But `packages/db/src/seed.ts:60-68` seeds only 9 Tier A agents (no `architect`), and `apps/web/lib/fixtures.ts:7-15` `tierAFixture` lists only 9 — so `/agents` + `/studio` render 9. `architect.svg` exists in `packages/agents/avatars/` but is **missing** from `apps/web/public/avatars/`. Three sources of truth diverge silently. | high | → Round-2 backlog card: "roster-three-source-parity" (sync architect into seed.ts + fixtures.ts + copy avatar to public/avatars; already half-tracked as task_9c158c18) |
| **Emoji drift between fixtures and the fiche/seed source of truth.** `fixtures.ts:13` gives Quality Controller `✅` and `:15` gives Agent Evaluator `⚖️`, but the authoritative fiche frontmatter + seed use `🎯` and `📊`. Same agent, two faces depending on whether the UI reads DB or static fixtures. | low | → Round-2 backlog card: fold into "roster-three-source-parity" (align fixtures emoji to fiche/seed) |
| **No machine-checked single-responsibility / mandate test.** agent-patterns.md:73-76 (RES-035) makes the "3 mandatory agent criteria" (written mandate, decides in scope, clear escalation) the gate for *creating* an agent. `fiche-quality.test.ts:20` enforces the four §12 body sections + cited source + Verdict vocabulary — strong — but nothing asserts `escalate_when`/`role` express a *single* mandate; an over-broad `domains: [all]` fiche passes. Authoring-discipline gap, not a live defect. | low | → charte (document the single-responsibility self-audit as a fiche-authoring step in AGENTS.md §2) |

**High/blocking in this pillar:** Roster divergence — fiches (10) vs seed/fixtures (9), `architect` absent from both runtime sources and its avatar missing from `apps/web/public/avatars/`, so the cockpit shows 9 agents while doctrine + fiche layer claim 10.

## Pillar 4 — Agent↔agent communication (loop events, handoffs)
| Constat | Gravité | Remédiation |
|---|---|---|
| Event vocabulary (30+ types) is persisted as a free-form `text('type').notNull()` column (`schema.ts:186`) and emitted as scattered string literals (`dispatch.ts`, `review-phase.ts`, `mission-events.ts`) — no shared literal-union / `as const` registry. This is the exact case CLAUDE.md §7 mandates ("prefer literal unions over enum"). A typo like `'producer_regressed_no_diff'` vs a consumer's expected string fails silently — `daily-report.ts:69` and `loadBlockedWindows` (`mission-events.ts:75`) match on bare string equality with zero compile-time guarantee. | medium | → Round-2 backlog card: "event-vocabulary-literal-union" (centralize a `MissionEventType` union, typed `logEvent`, SSE-safe) |
| Loop/handoff diagnostic events are emitted but **not observable** in the operator-facing daily report. `producer_regressed_no_diff` (`dispatch.ts:544`), `review_iteration` (`dispatch.ts:553`) and `delegation_fallback` (`dispatch.ts:505`) are written to the events table, yet `buildDailyReport` (`daily-report.ts:65-74`) only tallies `task_done`, `mission_*`, and `agent_evaluation`. A producer stuck regressing or a Tier-B fiche silently falling back never surfaces in the autopilot resume report — contradicting CLAUDE.md §4 autopilot "Report on resume". | medium | → Round-2 backlog card: "eval-loop-health-in-daily-report" |
| The evaluator-optimizer handoff is **asymmetric / dual-channel**. Reviewers expose a typed `priorFindings: string[]` field (`reviewers.ts:126,138-139`), but the live Tier-B correction loop bypasses it: findings are re-injected into the *producer* via raw string concat through `skillContext` (`dispatch.ts:466-473,532`), while the gate's own `realCodeReviewer` is re-invoked with **no** memory of its prior verdict. The critic re-judges each iteration statelessly, so the loop can oscillate without the reviewer knowing it already flagged the issue — weaker than the agent-patterns "isolate context + structured result" contract (agent-patterns.md:143). | medium | → ADR 0008 (document the producer-only findings channel as the intended contract, or unify on typed `priorFindings`) |
| QC-BLOCK short-circuit drops the sec/reviewer/evaluator handoff **without an observability trace**. `runCriticGates` returns early on a QC BLOCK (`review-phase.ts:25`), so the sec-reviewer, code-reviewer and advisory agent-evaluator never run and emit **no** "skipped-because-QC-block" event. A trace reader sees `quality_control_verdict=BLOCK` then nothing, with no signal that the downstream critic chain was intentionally bypassed vs. crashed. | low | → Round-2 backlog card: "critics-skipped-event-on-qc-block" |
| Loop bound is correct but its rationale lives only in code comments, not in doctrine. `MAX_REVIEW_ITERATIONS = 2` (`dispatch.ts:462`) plus the budget-projection break (`dispatch.ts:529-530`) correctly implement bounded-iteration / circuit-breaker (agent-patterns.md; production-patterns.md:101). But neither `AGENTS.md` nor an ADR records the bound as the governing contract — the invariant is one refactor away from silent loss. | low | → ADR 0008 (record the bounded evaluator-optimizer contract: cap = 2, dual break on cap+budget, regression-stop) |

**High/blocking in this pillar:** none. The communication mechanics are functionally sound (bounded loop, `last_message` handoff, prompt chaining via `upstreamResults`, regression-stop — all real and tested). The gaps are typing discipline, observability, and doctrine-grounding of an otherwise working contract.

## Pillar 5 — Skill creation (lifecycle structure, L1/L2)
| Constat | Gravité | Remédiation |
|---|---|---|
| All 7 authored skills carry the **full lifecycle body** required by CLAUDE.md §12 (Principles citing a source → When to Use/NOT → Process numbered → Output Schema → Rationalizations Table → Red Flags → binary Verification Criteria). E.g. `mas-mission-planner/SKILL.md:22-110`, `mas-reviewer/SKILL.md:23-114`, `mas-sec-reviewer/SKILL.md:24-107`, `intake-audit/SKILL.md:24-115`. **Zero stub skills** — the known anti-pattern (skills-reference.md:74-92) is absent. | low | → charte (record this as the conformant baseline in STRUCTURE.md; no fix needed) |
| **`intake-audit` is a full-quality authored skill but is NOT in the L1 indexing pipeline.** `ORCHESTRATOR_SKILL_IDS` lists only 6 ids (`scanner.ts:12-19`); `reindex.ts:11` scans only that list, so intake-audit gets **no `data/skill-cache/.../summary.md` and no `skills` DB row**. Its only mention in `packages/` is a prose string in `packages/memory/src/intake.ts:82`, i.e. invoked by name but never machine-discoverable by the router. The 7th skill is effectively invisible to L1 progressive disclosure. | medium | → Round-2 backlog card: "wire-intake-audit-into-L1-index" (add to `ORCHESTRATOR_SKILL_IDS` or document as Claude-Code-only) |
| **`intake-audit` summary is L1-overweight relative to the others.** Its `summary:` is ~626 chars / ≈156 tokens vs ≈87–92 tokens for the six orchestrator skills (`intake-audit/SKILL.md:6`). It stays under the ≈200-token / 800-char `SUMMARY_MAX_CHARS` ceiling (`scanner.ts:36`), but bundles guardrail clauses that belong in the L2 body, diluting the L1 précis purpose (skills-reference.md:200). | low | → Round-2 backlog card: "trim-intake-audit-L1-summary" |
| **L1/L2 cap is enforced by silent truncation, not validation.** `scanner.ts:79` and `:143` do `.slice(0, SUMMARY_MAX_CHARS)` — an over-length summary is chopped mid-sentence rather than failing reindex with a warning. The ≤200-token rule (CLAUDE.md §6/§12) is a soft trim, not a checked gate. | low | → Round-2 backlog card: "reindex-warn-on-summary-overflow" |
| **SKILLS_REGISTRY.md policy layer omits the 7 authored skills entirely.** Its tier tables (`SKILLS_REGISTRY.md:38-60`) list only third-party/plugin skills; no `mas-*` orchestrator skill or `intake-audit` appears, despite the file claiming to be "the policy layer: when each skill loads, who uses it" (`:35`). The doc that should govern our own skills is silent on them. | medium | → ADR 0008 / Round-2 backlog card: "register-own-skills-in-SKILLS_REGISTRY" |

**High/blocking in this pillar:** none. Skill **body quality is exemplary** — all 7 fully conform to the mandated lifecycle structure with no stubs (the central risk this pillar checks). The gaps are plumbing/documentation, not authoring.

## Pillar 6 — Skill selection (router, three-tier)
| Constat | Gravité | Remédiation |
|---|---|---|
| The skill's headline contract — three-tier **risk→model** routing (`risk_high→opus / risk_medium→sonnet / risk_low→haiku`, `mas-skill-router/SKILL.md:24-31,58,84-85`) — has **no runtime implementation**. `finalRisk` is computed (`dispatch.ts:175-180`) but only gates the sec-reviewer; the executed model is `proj?.defaultModel ?? 'claude-haiku-4-5'` (`dispatch.ts:597,774`), and the live router (`config/model-routing.json:35-45`) routes by **domain→provider**, not risk→model. A `risk:high` task on a haiku-default project silently runs on haiku. | high | → ADR 0008 (record the divergence + decide: implement risk-tier model selection in dispatch, OR amend the skill to describe the actual domain-based routing) |
| The skill's documented output `SkillRouterDecision` (`requiredSkills`, `favoriteSkills`, `tierBAgents`, `budgetEstimate.{tokens,model}`, `requires_validation`, `SKILL.md:62-77`) does **not exist in code**. The real selection returns `SelectResult = {skillIds, rationale, degraded}` (`select.ts:51-55`). The "max 3 / max 2" caps and `budgetEstimate.model` are uncoded; selection size is just `n` (default 5, `select.ts:58`). Fiche ↔ implementation describe two different systems. | high | → ADR 0008 (reconcile the fiche's schema/caps with `SelectResult`) + Round-2 backlog card: "align-skill-router-fiche-to-SelectResult" |
| Arsenal retrieval correctness is sound where it runs (L1-only: `router.buildPromptContext` injects `meta.summary` only, `router.ts:46-56`; QMD id-shape normalization `mas-arsenal/skill/<slug>.md`→router slug drops agent/rule/command stubs, `select.ts:154-158`), **but** the bulk of the 877-skill cold arsenal collapses to `domain:'planning'` (unmapped clusters + cyber:* fallback, `scanner.ts:31,129-131`; `library.ts:210-211`). So `scope.domain` filtering is coarse for most of the arsenal — recall leans on cluster-prefix + tag overlap + the optional semantic retriever, not domain. | medium | → Round-2 backlog card: "arsenal-cluster-to-domain-taxonomy" |
| The single MCP path for an agent to call QMD `query` live (ADR 0007 §Décision-5) is **câblage-only**: `selectLLM` deliberately omits the `mcp` field (`mission-llm.ts:156-159`), so no dispatch caller can activate it. ADR 0007 was amended (2026-06-25, F1) to defer this to Étape 1 — ADR and code are consistent, but the "an agent calls `query` during a mission" proof remains a unit test only. | low | → Round-2 backlog card already exists (`docs/backlog/arsenal-mcp-runtime-activation.md`); note in STRUCTURE.md that runtime arsenal-MCP is câblage-only pending Étape 1 |

**High/blocking in this pillar:** two **high** — (1) the prescribed three-tier risk→model routing is unimplemented (a `risk:high` task can silently run on haiku); (2) the skill fiche's `SkillRouterDecision` output schema + caps have no code counterpart. Both are fiche-vs-code divergences ADR 0008 must resolve.

## Pillar 7 — Main orchestrator (dispatch, runDispatchTick)
| Constat | Gravité | Remédiation |
|---|---|---|
| **No task_timeout / heartbeat circuit breaker.** `production-patterns.md:105,167` mandate a 5-min heartbeat → `mark failed, retry or escalate`. The orchestrator has none: a task is claimed `running` (dispatch.ts:682) and there is no clock, no watchdog, no reclaim path in `dispatch.ts` / `dispatch-tick.ts` / worker `index.ts`. An LLM call that hangs (`delegateWithDiff`/`llm.call` are unbounded) pins the worker `tick` forever. | high | → Round-2 backlog card: "stale-running-task-reclaim" (heartbeat circuit breaker) |
| **A crashed/throwing task is a permanent deadlock AND a budget leak.** If `executeTaskWithLLM` throws, the task stays `running`: `selectRunnableTasks` only re-picks `status==='todo'` (dispatch.ts:323), mission-complete needs `every==='done'` (dispatch.ts:666) → the mission can never finish. Worse, `reservedInFlight` sums `budgetTokens` of every `running` task (budget-gate.ts:131-137) → the orphaned reservation is never released, so one crashed task silently shrinks (and can exhaust) the **global** dispatch budget gate. No `try/catch` resets `running`→`todo`/`failed`. | high | → Round-2 backlog card: "release-reservation-and-requeue-on-failure" |
| **`Promise.all` in the tick loses healthy missions' progress on one failure.** `runDispatchTick` advances selected missions via `Promise.all` (dispatch-tick.ts:117-122). `executeNextTask` is **not** wrapped per-mission, so one mission's rejection rejects the whole batch; `safeTick` then swallows it (worker index.ts:97-107), discarding partial `advanced` results of every healthy mission in that tick. `production-patterns.md:92` warns to bound uncoordinated parallelism; `allSettled` is the correct primitive. | medium | → Round-2 backlog card: "per-mission-isolation-in-tick" (allSettled) |
| **`dispatch.ts` is 821 lines — over the <800 charter ceiling** (CLAUDE.md §7). It carries planMission, executeNextTask, both run paths, the review/evaluator loop, risk gate, and resume-after-validation. The single hottest file, over the binary threshold. | low | → Round-2 backlog card: "resplit-dispatch-under-800" |
| **No `task_timeout` event type / fallback wiring vs documented circuit breakers.** `production-patterns.md:101-106` lists three obligatory breakers: `budget_exceeded` (✓ implemented, dispatch-tick.ts:86), `rate_limit → fallback provider` (partial — router fallback exists), and `task_timeout (5 min)` (**absent**). The orchestrator implements 1 of 3 named breakers. | medium | → ADR 0008 (record orchestrator vs production-patterns circuit-breaker coverage: 1/3 named breakers present) |

**Strong positives (for ADR 0008 / STRUCTURE.md, not deficiencies):** atomic task claim via conditional `UPDATE … WHERE status='todo'` (dispatch.ts:680-685) correctly serializes concurrent executors; `selectForTick` is pure/deterministic and unit-testable (dispatch-tick.ts:41-74); the pre-flight budget gate counts both logged spend **and** in-flight reservation (budget-gate.ts:55-78,131-137); the §5 risk gate (`high`/`blocking` → `pauseForRiskGate`) runs before any LLM call (dispatch.ts:697-699); the evaluator-optimizer loop is bounded by **both** an iteration cap and the task token budget (dispatch.ts:525-530).

**High/blocking in this pillar:** two **high** — (1) no task_timeout/heartbeat circuit breaker (worker can hang indefinitely); (2) crashed-task deadlock + unreleased budget reservation (one failed task can permanently stall a mission and shrink/exhaust the global budget gate).

## Pillar 8 — Docs structure (taxonomy, naming, the mess §1 names)
| Constat | Gravité | Remédiation |
|---|---|---|
| Folder name contains a literal **space**: `docs/claude doc/` exists (confirmed `ls -d "docs/claude doc"`; `.gitignore:71-73` escapes it as `docs/claude\ doc/`). Directly violates the target naming rule (kebab-case, zéro espace dans les chemins — design-spec §7) and is the exact mess §1 names ("dossier `claude doc/` espace interdite dans le nom"). | high | → Round-2 step-0 rename (to `docs/claude-doc/` or fold into `docs/resources/`) + charte (STRUCTURE.md) records the kebab-case/no-space rule |
| **French/English drift** in canonical folder names: source corpus folder is spelled `docs/ressources/` (French) per `.gitignore:75-80`, while the charter's locked target is English `docs/resources/` (design-spec §6). Two spellings = two mental models, broken cross-refs once renamed. | medium | → Round-2 step-0 rename (`ressources` → `resources`) + charte (STRUCTURE.md) fixes English as the path language |
| **No structure/convention charter exists**: `ls docs/STRUCTURE.md` → "No such file or directory", and `grep STRUCTURE.md CLAUDE.md` returns nothing — yet design-spec §7 requires `docs/STRUCTURE.md` as the single source of truth for docs+code, referenced from CLAUDE.md §3/§7. The mess §1 cites "aucune règle de nommage, aucun document de structure/convention". | high | → charte (STRUCTURE.md) is created in Round 1 (this pillar most directly motivates it) + CLAUDE.md §3/§7 back-reference added (Round-2) |
| **No taxonomy field on any doc**: `grep -rl "^lane:" docs/` → 0 hits. The target backbone is a `lane` frontmatter field (~7 voies), not renamed folders (design-spec §6); today there is zero typed-spine metadata, so QMD/agents cannot route by lane. | medium | → ADR 0008 (fiche contract + typed-spine enums) records the `lane` field; Round-2 backlog card: "backfill-lane-frontmatter" |
| **Raw and distilled content are mixed, no admission gate**: design-spec §1 describes `docs/ressources/` as 44 PDF + 44 `.md` mélangés (folder absent from this checkout — only `.gitignore:76` `docs/ressources/*.pdf` + `:80` `docs/ressources/md/` materialize it here). Target separates raw (`docs/resources/` → QMD `mas-resources`) from distilled (`docs/knowledge/` → `mas-knowledge`). No `archive`/`maps` lanes exist yet (`find docs -maxdepth 2 -type d`). | medium | → ADR 0008 (raw/distilled seam + capture contract) + Round-2 backlog card: "split-raw-vs-distilled-add-archive-maps-lanes" |
| **Inconsistent file naming inside the corpus folder**: the 7 committed `.md` in `docs/claude doc/` use spaces, apostrophes and accents (e.g. `3 checks a faire avant d'upgrader ton modele IA.md`, `système-qui-fai-tourner-une-distribution-claude.md`) — direct breach of kebab-case/ISO-date naming (design-spec §7). By contrast `docs/learning/` and `docs/intake/` already follow `YYYY-MM-DD-slug` cleanly. | low | → Round-2 step-0 rename (normalize the 7 files to kebab-case) governed by charte (STRUCTURE.md) |

**High/blocking in this pillar:** two high — (1) the space-in-path folder `docs/claude doc/`; (2) the absent `docs/STRUCTURE.md` charter (unreferenced in CLAUDE.md). These two most directly motivate the STRUCTURE.md charter for Round 1.

## Pillar 9 — Code architecture & design-patterns (monorepo, llm.ts injection, ports/adapters)
| Constat | Gravité | Remédiation |
|---|---|---|
| **Single LLM injection seam is clean and well-guarded.** `packages/core/src/llm.ts:37-39` defines the `LLMClient` port; the only real Anthropic-SDK call site is `packages/core/src/llm.real.ts:24` (`import { query } from '@anthropic-ai/claude-agent-sdk'`), the sole SDK dependency in `packages/core/package.json`. No other file in `packages/`/`apps/` imports the SDK (grep). The guard `scripts/lint-no-sdk-payg.sh:22` matches every import form and is wired into `pnpm lint` (`package.json:15`). Textbook ports-and-adapters + billing isolation (CLAUDE.md §11). | low | → charte (STRUCTURE.md): grave the single-seam + guard convention as a load-bearing invariant, not just §11 prose |
| **Ports/adapters for multi-provider is exemplary.** `packages/core/src/providers/factory.ts:30` resolves a `RouterLLMClient` over a `Map<string, LLMClient>` of swappable adapters (`gemini.ts`, `openai.ts`, `perplexity.ts`, `claude-account.ts`), each behind the same port; provider SDKs are confined to `providers/` by the same guard (`lint-no-sdk-payg.sh:38-46`). Default-OFF when no source enabled (`factory.ts:40`) matches §11.bis. Bounded monorepo: every `@mas/*` package has a single responsibility and clean `workspace:*` deps; no cycles observed. | low | → charte (STRUCTURE.md): document the adapter-per-provider + RouterLLMClient seam as the canonical extension pattern |
| **`packages/agents/src/dispatch.ts` is 821 lines — over the §7 800-line file ceiling.** Verified: 821 on HEAD, 765 on `main`. The only non-test source file above the threshold (next largest `memory/src/retriever.ts:481`). Memory recorded F-SIZE-1 as "RESOLVED (739 on main)", but the file has since regrown past the bar — the per-function splits in #48 added net lines without re-splitting the file. ~12 distinct concerns (`dispatch.ts:99-815`). | medium | → Round-2 backlog card: "resplit-dispatch-under-800" (same card as Pillar 7 row 4; dedupe) |
| **Co-located `.test.ts` convention is real and largely honoured, with one thin spot.** `packages/agents/src` (32 tests / 20 src) and `db` (6/6) are well covered; `packages/skills/src` ships only 4 tests for 9 source files — the lowest ratio, and skills is the live arsenal-routing path (`select.ts` carried a silently-dead retriever bug fixed in #45). The §7 convention is sound; the gap is enforcement. | low | → charte (STRUCTURE.md): grave the co-located `.test.ts` rule; → Round-2 backlog card: "backfill-skills-test-colocation" |
| **`api-fallback/` escape hatch is documented but absent — the forbidden path is correctly a no-op.** CLAUDE.md §11 + `lint-no-sdk-payg.sh:30` carve out `packages/core/src/api-fallback/` as the only legal home for `@anthropic-ai/sdk`, but that directory does not exist (`ls` → No such file or directory). This is the *correct* state (PAYG unbuilt), but the guard's allowlist references a path that never materialized — a reader can't tell "intentionally empty" from "deleted". | low | → charte (STRUCTURE.md): note `api-fallback/` as a deliberately-empty, guard-reserved socket (completeness-over-YAGNI) |

**High/blocking in this pillar:** none. **Validation of the design-spec stance (§7 "code architecture is CLEAN → grave it"):** confirmed. The single LLM seam, ports/adapters provider layer, billing guard wired into `pnpm lint`, bounded acyclic `@mas/*` monorepo, and co-located tests deserve to be graved in STRUCTURE.md as invariants. The one real challenge to "clean": `dispatch.ts` quietly regrew to 821 lines — the single concrete architecture-hygiene debt.

## Synthèse — top findings by gravité, and what each one feeds

### Blocking (1)
| # | Pillar | Finding | Feeds |
|---|---|---|---|
| B1 | 2 — Task decomposition | The production planner is a hardcoded mock (`mockMissionPlanner`, dispatch.ts:239 / llm.ts:131-220); natural-language missions are never actually decomposed by an LLM. The entire mission-planning doctrine is documented but unwired. | Round-2 backlog: "wire-real-mission-planner" |

> Note on B1's nature: this is the single most consequential finding, but it is **not a defect to fix in Round 1** — Round 1 is doc-only (specs + decisions). It is the headline reason the Round-2 build exists. It is recorded here so the charter/ADR and the Round-2 backlog inherit it explicitly rather than discovering it mid-build.

### High (8)
| # | Pillar | Finding | Feeds |
|---|---|---|---|
| H1 | 1 — Memory | No consolidation / dedup / decay; registers grow append-only forever. | Round-2 card: "memory-consolidation-and-dedup" |
| H2 | 2 — Task decomposition | `clarifyingQuestions` (ask-before-plan) has no producer and no consumer — missions can never pause for clarification. | Round-2 card: "clarifying-questions-gate" |
| H3 | 3 — Agent creation | Roster three-source divergence: 10 fiches vs 9 in seed & fixtures; `architect` missing from runtime + its avatar missing from `public/avatars/`. | Round-2 card: "roster-three-source-parity" |
| H4 | 6 — Skill selection | Prescribed three-tier risk→model routing is unimplemented; a `risk:high` task can silently run on haiku. | ADR 0008 (decide build-or-amend) |
| H5 | 6 — Skill selection | `SkillRouterDecision` output schema + caps in the fiche have no code counterpart (real shape `{skillIds, rationale, degraded}`). | ADR 0008 + card: "align-skill-router-fiche-to-SelectResult" |
| H6 | 7 — Orchestrator | No task_timeout / heartbeat circuit breaker — a hung LLM call pins the worker tick forever. | Round-2 card: "stale-running-task-reclaim" |
| H7 | 7 — Orchestrator | A crashed/throwing task is a permanent mission deadlock **and** leaks its budget reservation (shrinks/exhausts the global budget gate). | Round-2 card: "release-reservation-and-requeue-on-failure" |
| H8 | 8 — Docs structure | Two structural breaches: the space-in-path folder `docs/claude doc/` and the absent `docs/STRUCTURE.md` charter (unreferenced in CLAUDE.md). | charte (STRUCTURE.md, Round 1) + Round-2 step-0 rename |

### What the audit confirms is already strong (graved, not fixed)
- **Code architecture (Pillar 9)** is genuinely clean: single LLM seam (`llm.ts`/`llm.real.ts`), ports/adapters provider layer, billing guard wired into `pnpm lint`, bounded acyclic monorepo, co-located tests → all to be **graved as invariants** in STRUCTURE.md.
- **Skill body quality (Pillar 5)** is exemplary — all 7 authored skills carry the full §12 lifecycle structure; zero stubs.
- **Orchestrator safety primitives (Pillar 7)** that DO exist are correct: atomic task claim, deterministic tick selection, dual-counted budget gate, §5 risk gate before any LLM call, dual-bounded evaluator-optimizer loop.

### Severity routing summary
- **→ charte (STRUCTURE.md):** naming rules (kebab/no-space), the clean code-architecture invariants (single seam, ports/adapters, co-located tests, `api-fallback/` reserved socket), planner-output canonical-schema note, fiche single-responsibility self-audit step, memory SessionStart-hook gap note, arsenal-MCP câblage-only note.
- **→ ADR 0008:** `lane` typed-spine + raw/distilled seam; risk→model routing decision (build-or-amend); skill-router schema reconciliation; bounded evaluator-optimizer contract; producer-only findings-channel contract; circuit-breaker coverage (1/3) note; risk-gating-contingent-on-real-planner note; capture-failed observability.
- **→ Round-2 backlog cards:** wire-real-mission-planner · clarifying-questions-gate · planner-acyclicity-validation · memory-consolidation-and-dedup · memory-candidate-trust-and-scoring · runtime-capture-all-five-registers · roster-three-source-parity · fiche-skill-cap-enforcement · event-vocabulary-literal-union · eval-loop-health-in-daily-report · critics-skipped-event-on-qc-block · wire-intake-audit-into-L1-index · trim-intake-audit-L1-summary · reindex-warn-on-summary-overflow · register-own-skills-in-SKILLS_REGISTRY · align-skill-router-fiche-to-SelectResult · arsenal-cluster-to-domain-taxonomy · stale-running-task-reclaim · release-reservation-and-requeue-on-failure · per-mission-isolation-in-tick · resplit-dispatch-under-800 · backfill-lane-frontmatter · split-raw-vs-distilled-add-archive-maps-lanes · backfill-skills-test-colocation.
