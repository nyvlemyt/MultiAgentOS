# Shard — cluster `skill:core-agent`, lot H (Doer H)

Source: `affaan-m/ecc` (MIT). Inspection copies at `/tmp/ecc-inspect/skills/<slug>/` (read-only).
Output skills at `packages/skills/library/<slug>/SKILL.md`. Audit barre LARGE (intake-audit T0/T1/T2).
Sanitize sweep run on all 5 sources: zéro AKIA/JWT/clé privée/token GitHub, zéro PII gmail/IP privée, zéro `@anthropic-ai/sdk` import.
**Exception §11 traitée :** `security-scan` source contenait `export ANTHROPIC_API_KEY=` + un mode `--opus` PAYG + un scanner externe non-pinné (`npx ecc-agentshield`) + une GitHub Action d'upload → machinerie unsafe **strippée** (maintainer-safe), lens conservé en local-only. Pas un reject : le lens a une valeur propre et aucun équivalent dans nos assets.

| slug | décision | raison | dedup | chemin |
|---|---|---|---|---|
| `ralphinho-rfc-pipeline` | implement_now (T1) | Discipline d'intégration riche (work units vérifiables, tiers de complexité, merge queue refusant les deps en échec, recovery par éviction/re-scope). Performant, valeur propre forte. | Recoupe `mas-mission-planner` (décompose en DAG) mais angle distinct : ce n'est pas la planification, c'est la *vérification par unité + merge queue + re-intégration après chaque merge* posée par-dessus le DAG. Pas une dup-no-better. | `packages/skills/library/ralphinho-rfc-pipeline/SKILL.md` |
| `recursive-decision-ledger` | implement_now (T1) | Gem signalé — audité avec soin. Ledger append-only des rollouts + promotion gate qui traite la confiance récursive comme preuve, jamais comme approbation (mappe §5). Très utile pour nos décisions/ledger. Valeur propre forte. | Pas de skill ledger/rollout dans `our-assets-index.md`. Distinct de `intake-audit` (one-shot) et `mas-memory-keeper` (faits durables). Pas une dup. | `packages/skills/library/recursive-decision-ledger/SKILL.md` |
| `safety-guard` | implement_now (T1) | Couche d'enforcement runtime (PreToolUse hooks : Careful/Freeze/Guard) qui mécanise §5/§4. Bloque rm-rf/force-push/écritures hors scope/leakage cross-projet, log les blocages. Performant, valeur propre. | Recoupe §5 (policy) et `mas-sec-reviewer` (verdict par tâche) mais angle distinct : enforcement à l'exécution, pas la policy ni le verdict — défense en profondeur. Pas une dup-no-better. Reframé sur §5/§4/§8 (log dans `data/` au lieu de `~/.claude/`). | `packages/skills/library/safety-guard/SKILL.md` |
| `santa-method` | implement_now (T1) | Vérification adversariale à DEUX reviewers indépendants (context-isolés, même rubric) + boucle fix-until-nice bornée. Casse le biais partagé de l'auto-review (un défaut vu par un seul reviewer = vrai défaut). Riche, valeur propre. | Recoupe `mas-reviewer`/`quality-controller` (verdict à porte unique) mais angle distinct : dual-reviewer bias-breaking pour output shippable haut-enjeu. Écho du pattern repowise déjà dans `skills-reference.md`, durci. Pas une dup. | `packages/skills/library/santa-method/SKILL.md` |
| `security-scan` | adapt_now (T1) | Lens audit config `.claude/` (secrets, allow-lists permissives, injection hooks, MCP risqués, fiches agents >7 tools) très utile pour onboarding/hygiène. Réimplémenté **maintainer-safe** : NO ANTHROPIC_API_KEY, NO `--opus` PAYG, NO scanner externe non-pinné, NO upload tiers ; checks regex/AST locaux (réutilise le set Sanitize de `intake-audit`). Pas reject — valeur propre, aucun équivalent. | Recoupe `mas-sec-reviewer` (verdict runtime par tâche) et le lint guard §11 (no `@anthropic-ai/sdk`) mais angle distinct : audit de la *surface de config au repos*, ingestion-time. Pas une dup. | `packages/skills/library/security-scan/SKILL.md` |

## Notes
- 5/5 keepers (4 implement_now + 1 adapt_now), 0 rejet. Aucun stub-no-content irrécupérable ; toutes les sources portaient un lens opérationnel.
- **§11 / KILL :** seul `security-scan` déclenchait un trigger KILL (ANTHROPIC_API_KEY + PAYG `--opus` + npx externe). Traité par la règle maintainer-safe d'intake-audit (strip machinerie, garde lens, local-only) → `adapt_now`, conforme. Aucun autre slug ne touche secrets/PAYG.
- **Prompt Defense Baseline** ajouté en tête de body sur les 5 (skills susceptibles de piloter un agent : pipeline d'orchestration, ledger de décision, enforcement de sécurité, dual-review, audit de config).
- 7 sections riches §12 sur chaque skill (Overview → When to Use/NOT → Principles[source] → Process → Rationalizations → Red Flags → Verification binaire). 1ère ligne body = commentaire source. Frontmatter `name/description(triggers + Do NOT)/summary` + `metadata: origin/license MIT/cluster/tier T1/status library`. `santa-method` : origin crédite l'auteur original (Ronald Skelton, RapportScore.ai) via affaan-m/ecc.
- Grep final sur les 5 outputs : 0 import `@anthropic-ai/sdk`, 0 secret live. (Le seul match `@anthropic-ai/sdk` est la prose descriptive « no @anthropic-ai/sdk » du lint guard §11 dans le summary de `security-scan` — pas un import, pas du code runtime.)
- Contrainte respectée : écrit UNIQUEMENT les 5 library SKILL.md + ce shard. Pas de ledger.tsv, pas d'autres shards.
