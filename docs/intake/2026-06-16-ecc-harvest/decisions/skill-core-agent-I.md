# Shard — cluster `skill:core-agent`, lot I (Doer I)

Source: `affaan-m/ecc` (MIT). Inspection copies at `/tmp/ecc-inspect/skills/<slug>/`.
Output skills at `packages/skills/library/<slug>/SKILL.md`. Audit barre LARGE (intake-audit T0/T1/T2).
Sanitize sweep run on all 4 sources: zéro secret, zéro PII, zéro chemin interne, zéro `@anthropic-ai/sdk`, zéro PAYG Anthropic.

| slug | décision | raison | dedup | chemin | état |
|---|---|---|---|---|---|
| `social-publisher` | reject (T0) | Verticale : pur wrapper d'un SaaS propriétaire (SocialClaw `getsocialclaw.com`) exigeant `SC_API_KEY` + envois sortants vers 13 plateformes = action risquée §5, et pousse l'install de paquets npm non-épinglés (`socialclaw`, `@xquik/tweetclaw`) + un credential tiers. Tout le skill EST le binding fournisseur ; retirer la machinerie d'envoi (maintainer-safe) ne laisse aucun lens transférable → coquille vide. Unsafe + stub-après-strip. | Aucun équivalent dans `our-assets-index.md` ; non gardé donc dedup sans objet. | — (pas de library SKILL.md) | rejeté — NE code aucun envoi (§5). Si un jour besoin de publication sociale : passer par `intake-audit` dédié + déclarer la catégorie d'action risquée dans `config/permissions.json`, jamais hardcoder l'envoi. |
| `team-agent-orchestration` | implement_now (T1) | Couche coordination d'équipe au-DESSUS du lifecycle : cartes Kanban (owner/scope/state/evidence/merge_gate), isolation worktree, merge sur preuve, control-pane, failure-modes (agent-soup/board-theater). Valeur propre forte, performant. | Recoupe planner/dispatcher mais angle DISTINCT = contrat d'équipe visible + discipline de merge (ownership/gates), pas une dup-no-better de `mas-mission-planner` (DAG) ni `mas-skill-router` (sélection). | `packages/skills/library/team-agent-orchestration/SKILL.md` | neuf (réécrit §12 complet 7 sections ; commentaire source ; summary L1 ; metadata complet ; Prompt Defense ajouté — skill pilote des agents ; maintainer-safe : aucune machinerie externe, gates §5 référencés ; 0 PAYG) |
| `team-builder` | implement_now (T1) | Composeur interactif d'équipe d'agents : découverte dynamique, menu par domaine, sélection flexible, cap 5, fan-out parallèle + synthèse. Opérationnel et complet. | Recoupe `mas-skill-router` mais angle DISTINCT = pick HUMAIN d'une escouade ad-hoc (vs router autonome) ; complémentaire, pas dup. | `packages/skills/library/team-builder/SKILL.md` | neuf (réécrit §12 complet ; commentaire source ; summary L1 ; metadata complet ; Prompt Defense ajouté — pilote/spawn d'agents ; maintainer-safe : spawn respecte autonomie §4 + gates §5, ne les contourne pas ; 0 PAYG) |
| `workspace-surface-audit` | implement_now (T1) | Audit read-only des capacités du workspace (repo/MCP/LSP/plugins/connecteurs/env par NOM) → gaps classés (available/primitive-only/missing) → forme recommandée. Riche, performant, valeur propre. | Adjacent à `intake-audit` (décide UN candidat → dossier) et `mas-context-manager` (pack projet) mais angle DISTINCT = scan de surface + recommandations qui ALIMENTENT intake-audit. Pas dup. | `packages/skills/library/workspace-surface-audit/SKILL.md` | neuf (réécrit §12 complet ; commentaire source ; summary L1 ; metadata complet ; Prompt Defense ajouté ; durci §5/§11 : jamais imprimer une valeur de secret, `ANTHROPIC_API_KEY` = smell pas capability, read-only ; 0 PAYG) |

## Notes
- 3 keepers (T1), 1 rejet (T0 `social-publisher`). Barre LARGE respectée : rejet uniquement car unsafe (binding SaaS payant + envois §5) ET stub-après-strip — aucun lens à sauver.
- `description` field : string YAML block-scalar avec triggers positifs ET négatifs (« Do NOT use ») conforme `skills-reference.md`.
- Prompt Defense Baseline ajouté aux 3 keepers : `team-*` pilotent/spawnent des agents, `workspace-surface-audit` lit des surfaces externes untrusted → tous éligibles au header anti-injection.
- `social-publisher` mentionnait `SC_API_KEY` (clé tierce SaaS) — aucune clé Anthropic ni `@anthropic-ai/sdk` ; le rejet est motivé par la verticale risquée + l'install non-épinglée, pas par une clé Anthropic.
- Contraintes respectées : écrit UNIQUEMENT les 3 library SKILL.md + ce shard. Aucun `ledger.tsv`, aucun autre shard touché.
