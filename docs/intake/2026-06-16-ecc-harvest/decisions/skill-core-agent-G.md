# Shard — cluster `skill:core-agent`, lot G (Doer G)

Source: `affaan-m/ecc` (MIT). Inspection copies at `/tmp/ecc-inspect/skills/<slug>/`.
Output skills at `packages/skills/library/<slug>/SKILL.md`. Audit barre LARGE (intake-audit T0/T1/T2).
Sanitize sweep run on all 6 sources: zéro secret, zéro PII, zéro `@anthropic-ai/sdk`, zéro PAYG Anthropic.

| slug | décision | raison | dedup | chemin | état |
|---|---|---|---|---|---|
| `openclaw-persona-forge` | reject (T0) | Vertical à un produit externe spécifique (OpenClaw « lobster soul »). Skill de création de persona-personnage (SOUL.md/IDENTITY.md, gacha.py) sans valeur pour la spine ni l'arsenal MAS — hors-domaine, pas un outil de cockpit multi-agent. Pas un §11-reject (clean), mais dup-no-better=non / stub=non / valeur-domaine-MAS=non → rejet pour fit nul. | Aucun équivalent dans `our-assets-index.md` — et aucun besoin : MAS n'est pas une fabrique de personas de mascotte. | — | rejeté |
| `opensource-pipeline` | reject (T0) | Lentille fork→sanitize→package DÉJÀ absorbée dans `intake-audit` (Phase B2, étape Sanitize 4.bis). Valeur résiduelle examinée = orchestration end-to-end (forker→sanitizer→packager + publish GitHub). Or elle dépend de sous-agents ECC inexistants en MAS (`opensource-forker/sanitizer/packager`), **copie** le code vers `$HOME/opensource-staging` et **push** sur GitHub — ce qui viole le principe MAS « jamais copier/cloner le code externe » (CLAUDE.md §1/§8) et touche §5 (git push). Le lens utile (sanitize indépendant, maintainer-safe) est déjà chez nous. | Sanitize stage = `intake-audit` §4.bis (regex secrets/PII/refs) ; pattern crédité `affaan-m/ecc agents/opensource-sanitizer.md`. Résidu fork+package = hors-scope MAS. | — | rejeté (absorbé dans intake-audit B2 ; résidu fork/package hors-scope — MAS ne copie pas le source projet) |
| `parallel-execution-optimizer` | implement_now (T1) | Discipline d'exécution parallèle (lane matrix, write-surface disjoint, polling des longues lanes, evidence par lane). Touche la spine : multi-mission/multi-projet concurrency du dispatcher. Performant, valeur propre forte. | Recoupe `mas-skill-router` (qui *route*) mais angle distinct = *sûreté de la parallélisation* (collisions de write-surface, lanes gated). Pas une dup-no-better. | `packages/skills/library/parallel-execution-optimizer/SKILL.md` | neuf (réécrit §12 complet : 7 sections, source comment, summary L1, metadata ; Prompt Defense ajouté — pilote l'exécution de lanes ; maintainer-safe : lanes destructrices/live restent gated §5, pas de background process qui survit au turn ; ancré sur §4/§5/§6) |
| `plan-orchestrate` | reject (T0) | Bridge génératif vers la commande ECC `/orchestrate` + catalogue d'agents ECC (`tdd-guide`, `python-reviewer`, etc.) + namespacing plugin/legacy ECC. Couplé bout-en-bout à des commandes et noms d'agents qui n'existent PAS en MAS. Le lens (plan→chaîne d'agents par step) recoupe `mas-mission-planner` (décomposition DAG + agent hints) sans le surpasser → dup-no-better. | Lens dup de `mas-mission-planner` (PlannerOutput JSON : tasks/deps/risk/agent hints). Le reste = tooling ECC inutilisable en MAS. | — | rejeté (tooling ECC-spécifique + dup-no-better du lens mas-mission-planner) |
| `prediction-market-oracle-research` | implement_now (T2) | Verticale domaine : marchés de prédiction comme signal oracle/forecasting. Self-contained, safe, source-grounded, jamais conseil d'investissement. Valeur domaine réelle (arsenal recherche/decision-intelligence). « garde si valeur domaine » → keeper. | Aucun équivalent dans `our-assets-index.md`. Complémentaire à `prediction-market-risk-review` (recherche vs gate). | `packages/skills/library/prediction-market-oracle-research/SKILL.md` | neuf (réécrit §12 complet : 7 sections, source comment, summary L1, metadata ; Prompt Defense ajouté — ingère des données de venue externes untrusted ; maintainer-safe : research-only, exécution déférée à risk-review + §5, disclaimer obligatoire) |
| `prediction-market-risk-review` | implement_now (T2) | Verticale domaine : gate de risque (5 portes : advice/venue/data-quality/security/privacy) avant tout workflow prediction-market touchant auth venue, portfolio, clés API, trade planning. Safe-by-default, mappe sur §5/§11. Complète l'oracle. | Analogue sectoriel du `mas-sec-reviewer` mais spécifique au domaine financier/marchés — pas une dup (gate domaine vs gate générique risque). | `packages/skills/library/prediction-market-risk-review/SKILL.md` | neuf (réécrit §12 complet : 7 sections, source comment, summary L1, metadata ; Prompt Defense ajouté — ingère données financières/venue untrusted ; maintainer-safe : jamais de clé privée/seed/password stockés, read-only par défaut, exécution bloquée → plan séparé + approbation humaine §5) |

## Notes
- Bilan : 3 keepers (T1×1, T2×2), 3 rejets. Barre LARGE respectée — les rejets sont fit-nul/dup-no-better/hors-scope, pas des rejets de confort.
- **Décision `opensource-pipeline`** : `reject (absorbé dans intake-audit B2)`. Le lens sanitize (fork→sanitize) est déjà intégré dans notre `intake-audit` §4.bis (étape Sanitize + crédit `agents/opensource-sanitizer.md`). La valeur résiduelle (fork→sanitize→package end-to-end + publish GitHub) est **non couverte** mais **non désirable** : elle copie le source externe vers un staging et pousse sur GitHub, ce qui contredit le principe MAS « jamais copier/cloner le code externe » (§1/§8) et touche §5 (git push). Aucune valeur résiduelle adoptable → rejet net.
- `description` field : conservé en string entre guillemets, triggers positifs + négatifs « Do NOT use » conformes à `skills-reference.md`.
- Prompt Defense Baseline ajouté aux 3 keepers : tous consommés par des agents traitant des données externes/untrusted (lanes d'exécution, données de venue financières) — hardening header verbatim en tête de body (intake-audit §8).
- Aucune clé Anthropic, aucun `@anthropic-ai/sdk`, aucun secret committé dans les 6 sources de ce lot.
