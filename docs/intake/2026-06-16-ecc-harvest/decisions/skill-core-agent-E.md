# Shard — cluster `skill:core-agent`, lot E (Doer E)

Source: `affaan-m/ecc` (MIT). Inspection copies at `/tmp/ecc-inspect/skills/<slug>/` (RO).
Output skills at `packages/skills/library/<slug>/SKILL.md`. Audit barre LARGE (intake-audit T0/T1/T2).
Sanitize sweep run on all 6 sources: zéro secret, zéro PII, zéro chemin interne, zéro `@anthropic-ai/sdk`, zéro PAYG Anthropic.
Note : les sources citent des noms de variables d'env placeholder (`X_BEARER_TOKEN`, `TRADING_WALLET_PRIVATE_KEY`, `ITO_API_KEY`, `EXA_API_KEY`) — ce sont des *références doc*, pas des valeurs committées ⇒ aucun CRITICAL. Aucune clé Anthropic.

| slug | décision | raison | dedup | chemin |
|---|---|---|---|---|
| `ito-data-atlas-agent` | reject (T0) | Dup-no-better : esquisse d'archi pour un agent de recherche HITL → draft de paniers prediction-market sur une venue propriétaire (Itô). Le lens générique (collecte→draft→revue humaine) EST déjà notre colonne (planner + dispatch + review gate §5) ; le reste est de la valeur spécifique-venue dont nous n'avons pas besoin. Mince (~65 l.), couplé à des skills/venues externes inexistantes chez nous. | Couvert par `mas-mission-planner` + dispatcher + `mas-reviewer`/review gate ; rien de neuf. | — (pas de library) |
| `latency-critical-systems` | adopt → implement_now (T2) | Lens d'ingénierie perf générique et solide : split des métriques (p50/95/99, freshness, queue, cache), hot-path mapping, ordre d'optimisation, backpressure, vérif par live readbacks. Performant, valeur propre. | Pas une dup de `Performance Benchmarker` (agent qui mesure) — ici c'est une *méthode* de design/optimisation réutilisable. | `packages/skills/library/latency-critical-systems/SKILL.md` |
| `lead-intelligence` | adapt → implement_now (T2) | Pipeline outreach riche (signal-scoring → warm-path → enrichment → drafts par canal, voice-matched). Lens à forte valeur domaine. Maintainer-safe : machinery d'égress/auto-send retirée, **drafts only**, sends gated §5, providers payants opt-in/default-OFF §11.bis, scraping privé hors scope. | Pas de dup ; `Trend Researcher` fait de l'intel marché, pas du lead-warm-path. | `packages/skills/library/lead-intelligence/SKILL.md` |
| `llm-trading-agent-security` | adapt → implement_now (T1) | Lens sécurité-agent (injection-comme-attaque-financière, spend limits déterministes indépendantes du modèle, simulation pré-send + min_amount_out obligatoire, circuit breakers, isolation hot-wallet, audit de toutes les décisions). Mappe directement sur §5 (sign/send/transfer = risk:blocking, gate humain) + `mas-sec-reviewer`. T1 utile (sécurité de la colonne). | Complémentaire à `mas-sec-reviewer` (gate runtime) — ici ce sont les *contrôles à construire autour* du gate, pas une dup. | `packages/skills/library/llm-trading-agent-security/SKILL.md` |
| `market-research` | adopt → implement_now (T2) | Méthode de recherche orientée-décision propre : sourcing de chaque claim, évidence contrarian + downside, séparation fact/inférence/recommandation, sizing top-down + bottom-up avec hypothèses explicites, quality gate avant livraison. | Pas une dup-no-better de `Trend Researcher` (rôle agent) — c'est une *skill méthode* réutilisable avec gate qualité, complémentaire. | `packages/skills/library/market-research/SKILL.md` |
| `ml-adoption-playbook` | adopt → implement_now (T2) | Méthodologie d'intégration ML dans une base non-ML (framing + mistake budget → data contract + anti-leakage → découplage API/fallback/feature-flag → baseline d'abord + eval vs baseline → handoff MLOps). Séquence l'agent : interface de découplage AVANT la boucle d'entraînement. Aligné §7 (TDD). | Pas une dup de l'agent `AI Engineer` (rôle) — c'est le *process* d'adoption ML, complémentaire. | `packages/skills/library/ml-adoption-playbook/SKILL.md` |

## Notes
- **5 keepers / 1 rejet.** Seul rejet : `ito-data-atlas-agent` (dup-no-better + venue-specific). Aucun stub irrécupérable parmi les 5 autres.
- **Prompt Defense Baseline** ajouté verbatim sur les 2 skills qui pilotent un agent : `lead-intelligence` (pipeline outreach) et `llm-trading-agent-security` (agent financier). Omis sur `latency-critical-systems` / `market-research` / `ml-adoption-playbook` (méthodes invoquées par l'utilisateur, non-pilotes — cohérent avec sibling `dashboard-builder`).
- **Maintainer-safe** appliqué :
  - `lead-intelligence` : retrait de l'égress/auto-send, drafts-only, sends `risk:high|blocking` gated (§5), providers tiers payants opt-in default-OFF (§11.bis), scraping privé/authentifié hors scope. Clés placeholder (X/Exa/Apollo) référencées en doc, jamais committées ; aucune clé Anthropic.
  - `llm-trading-agent-security` : tout send = `risk:blocking` → pause humaine via `mas-sec-reviewer` ; clés via env/secret manager, jamais code/logs (§11) ; skill = contrôles autour du gate, jamais un moyen de le contourner.
  - `latency-critical-systems` : live orders / migrations destructives / deploys impactant le client restent derrière un gate d'approbation (§5).
- `description` : conservée en string entre guillemets avec triggers positifs + « Do NOT use » (conforme `skills-reference.md`).
- 7 sections §12 présentes sur chaque keeper (Overview / When to Use / When NOT / Principles citant source / Process / Rationalizations / Red Flags / Verification Criteria binaires). 1ère ligne body = commentaire source `<!-- pattern from affaan-m/ecc … -->`. summary L1 ≤200 tok. metadata complet (origin/license/cluster/tier/status).
