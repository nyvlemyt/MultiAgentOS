# Rapport brut : attaque du plan révision 1, axe enchaînement des tâches

Agent : `relecteur-eve`, mode attaque. Lancé le 16/09/2026 sur `plan.md` révision 1. Déposé tel quel le 16/09 au soir depuis la transcription de session. Vérification contradictoire dans `attaques/attaque-plan.md`.

---

## Attaque du plan, axe enchaînement (11 findings, aucun fichier modifié)

Lu : `plan.md`, `design.md` (6 et 8), `attaques/attaque-spec.md`, `chantiers/PLAN.md`, `doctor.py`, `garde_git.py`, `garde_perimetre.py`, `_lib.py`, `settings.json`, `.claude/README.md`, `REGISTRE.md`, `journal.md`.

| # | Grav. | Étapes | Défaut | Scénario ordonné | Correctif dans le plan |
| --- | --- | --- | --- | --- | --- |
| E1 | HAUTE | 1.2 et 1.3 vers 1.5 et 1.6 | `CLAUDE_SONDE_SORTIE` n'atteint jamais le hook : rien dans le plan ne peut le poser. | 1.3 déclare la sonde ; 1.5 « pointe » la variable depuis un appel Bash, dont l'environnement ne persiste pas et n'est pas hérité par le processus que le harnais lance ; `os.environ["CLAUDE_SONDE_SORTIE"]` lève KeyError ; le hook sort en code 1, chaque `Glob` rend une erreur, aucun relevé n'est produit. Pire, 1.4 prescrit alors le mauvais remède : Melvyn redémarre la session, ce qui ne corrige rien. | Poser la variable dans le bloc `env` de `settings.json`, ou un défaut dans la sonde avec deux fichiers distincts fil/agent ; ajouter à 1.4 « si le relevé est vide, lire stderr du hook avant d'incriminer le rechargement ». |
| E2 | HAUTE | 1.1 vers 1.4 et 1.7 | L'unique copie de `settings.json` vit dans le scratchpad de session, et 1.4 planifie un redémarrage de session au milieu de la tâche. | 1.1 copie dans `$SCRATCHPAD` (chemin porteur d'un identifiant de session) ; 1.3 modifie `settings.json` ; 1.5 échoue (E1) ; Melvyn redémarre ; le nouveau `$SCRATCHPAD` est vide, 1.1 n'a noté que l'empreinte, pas le chemin absolu de la copie ; le poste tourne alors avec une sonde déclarée, cassée, et sans sauvegarde du seul fichier qui porte les trois verrous. | En 1.1, copier dans le dossier du chantier (hors git, durable), écrire le chemin absolu et l'empreinte au journal, et ajouter une étape 1.0 « si la session redémarre, restaurer d'abord, sonder ensuite ». |
| E3 | HAUTE | aucune vers 4.3, 4.5, 7.1 | L'état de départ E0, exigé par `design.md:73`, n'est produit par aucune tâche, alors que trois étapes le consomment. | 7.1 dit « comparé à ce qui a été sauvegardé avant la tâche 1 » ; rien ne l'a sauvegardé ; la comparaison se fait de mémoire. Aggravant, collision mission 0 : `.git/MERGE_HEAD` existe encore et `PLAN.md:69` annonce le commit imminent par Melvyn ; s'il commite pendant le lot, `git status --porcelain` passe de 13 lignes à 0 et `MERGE_HEAD` disparaît, donc 7.1 diverge et plus personne ne sait si l'écart vient d'un agent ou de Melvyn. | Tâche 0 explicite : `git status --porcelain`, `git diff`, `git diff --cached`, `git rev-parse HEAD`, `git worktree list`, `ls .git/MERGE_HEAD` dans un fichier daté du chantier ; 7.1 compare au dernier E0 et rebaseline explicitement quand Melvyn commite. |
| E4 | HAUTE | 4.2 vers 4.4 | Fenêtre où une fiche d'agent sans sa clause de périmètre est sur disque, visible de toutes les sessions du poste. | 4.2 écrit `.claude/agents/developpeur-eve.md` volontairement sans clause ; 4.3 vérifie, 4.4 ajoute la clause ; entre les deux, la contrainte « session mission 0 fermée » ne s'applique plus (tâche 1 seulement), donc une autre session peut invoquer un agent qui écrit du code sans borne. | Écrire la fiche avec la clause dès 4.2 et produire le rouge par une variante jetable hors `.claude/agents/` (ou fiche nommée `developpeur-eve-sans-clause.md` supprimée à la fin de 4.3) ; étendre « session mission 0 fermée » à toute la tâche 4. |
| E5 | HAUTE | 5.3 et 5.4 avec 6.3 | Dépendance circulaire : `doctor` contrôle la ligne au `REGISTRE.md` que seule la tâche 6 écrit. | 4 crée la fiche ; 5.3 pose `attendues = {..., "developpeur-eve"}` ; `REGISTRE.md:44-46` ne nomme que les quatre agents existants ; 5.4 attend `fiches d'agents : completes` et obtient une alerte ; l'opérateur croit à un bug de son contrôle et le rend tolérant, et le contrôle naît déjà désarmé. | Déplacer 6.3 avant 5.3, ou scinder. Corriger la ligne « Tâches 3, 4, 5 vers Tâche 6 » de la section Ordre. |
| E6 | MOYENNE | 3.3 vers 4.5 | Une fois la barrière hook posée, la fixture `refus` ne prouve plus la clause de la fiche. | 3.3 refuse `commit` quand `agent_id` est présent ; 4.5 joue `refus`, dont l'attendu est « refus motivé, aucun appel git » ; un agent qui a bien tenté le commit reçoit le refus du hook et peut rendre le même rapport qu'un agent obéissant, « aucun appel git » étant déclaratif. | Jouer `refus` avant la tâche 3, ou rendre l'attendu constatable (`git log --oneline -1` plus `git reflog -n 1` relevés par moi avant et après). |
| E7 | MOYENNE | 3.3 vers 3.4 | `garde_git.py` est relu à chaque appel par toutes les sessions, et la contrainte de session fermée ne couvre que la tâche 1. Le test de non régression de 3.1 appelle `decision(..., agent=False)` : il ne couvre jamais `main()`. | Entre l'édition et 3.4, la session mission 0 lance un `git status` : si l'édition est fautive, le hook sort autrement que 0 ou 2 et l'appel passe, verrou git muet. | Étendre la contrainte à la tâche 3 ; ajouter un test de bout en bout de `main()` par stdin (deux entrées JSON, avec et sans `agent_id`, codes 2 et 0 attendus). |
| E8 | MOYENNE | 1.0, 2.0, 1.4 | Les actions de la main de Melvyn sont dispersées dans les tâches et découvertes en cours de route, alors que le critère 8 plafonne les messages à trois. | Message 1 : ferme la session mission 0. Message 2 : crée la cible. Message 3 : redémarre la session. Le bloc jalon est le quatrième : le critère 8 est en échec avant le premier livrable. | Un bloc « ce que je te demande une seule fois, avant de commencer » en tête de plan, avec les commandes exactes ; les inscrire aussi dans `PLAN.md` section 3. |
| E9 | MOYENNE | 4.1 vers 4.5 | La fixture `vert` exige un rouge puis un vert, sans qu'aucune tâche ne produise l'espace de travail où l'agent a le droit d'écrire et de lancer des tests. | L'auteur doit inventer sur le moment le chemin du brief `vert` ; `garde_perimetre` autorise toute la racine, donc un chemin improvisé dans `data/` est accepté par le verrou ; l'écriture atterrit dans l'arbre où le merge de la mission 0 n'est pas commité. C'est G2 de l'attaque, revenu par la porte de la fixture `vert`. | Nommer dans le plan le dossier bac à sable des fixtures, le créer en 4.1, et écrire dans chaque brief la phrase « seul chemin autorisé en écriture : ... ». |
| E10 | BASSE | 7.5 vers lot 1b | Le contrôle vivant promis par `design.md:121` (présence de `agent_id`, avec date de revérification) n'est implémenté par aucune tâche. | Le jour où le harnais renomme le champ, `bool(entree.get("agent_id"))` vaut toujours False, le test unitaire reste vert, rien ne s'allume, et un agent peut commiter. | Ajouter à la tâche 5 un second contrôle, ou une ligne datée dans `securite.md` et au dashboard. |
| E11 | BASSE | 4.2 vers 4.4 | Aucune branche si le rouge n'est pas rouge. | Le plan ne fixe pas les outils de la fiche ; si `tools` omet Write et Edit, l'agent de 4.2 ne peut pas écrire, et on peut conclure à tort que la clause n'est pas nécessaire. | Fixer la liste `tools` en 4.1 avec son motif, et ajouter en 4.2 la branche « si l'agent refuse avant la clause, vérifier d'abord ses outils, puis consigner un rouge non concluant ». |

## Le point 4, la porte d'arrêt : vérifié, rien de bloquant

La porte de 2.5 est bien placée avant toute écriture d'agent, et la cible `%TEMP%\sonde-eve-perimetre.txt` est réellement hors périmètre : les racines autorisées sont la racine du projet, `C:\dev\bdfg-core`, la mémoire du bucket, `~/.claude/settings.json` et `$TEMP/claude` seulement (`garde_perimetre.py:52-61`). Une seule nuance : 1.6 lance un sous agent avant que la porte ait joué ; à placer après la tâche 2.

## Synthèse

Le plan est solide tâche par tâche et faible entre les tâches : la tâche 1 ne peut pas produire son relevé (E1), la tâche 7 compare à un état que personne n'a pris (E3), la tâche 5 attend de la tâche 6 (E5), et deux fenêtres laissent le poste sans barrière pendant que la session mission 0 peut reprendre la main (E2, E4).
