# Rapport brut : relecteur-eve, attaque du plan du lot 3, axe enchainement, 17/09/2026

Déposé tel quel par le fil. Brief : plan-lot-3.md, design-lot-3.md, _decisions/0011, doctor.py (sondes), test_gardes.py (noms de tests).

## Attaque du plan lot 3, axe enchainement

| Sévérité | Fichier:ligne | Constat | Preuve | Correctif proposé |
| --- | --- | --- | --- | --- |
| CRITIQUE | `plan-lot-3.md:17-19` (T4, T5, T6) et `:65-69` | Pendant une bascule, un verrou qui lève une exception laisse tout passer, et le plan ne le voit pas. Une édition en place (plusieurs `Edit` sur `garde_perimetre.py`, 21 Ko) met le fichier dans un état intermédiaire ; le `Edit` suivant est jugé par ce hook cassé. | `garde_git.py:229-233`, `garde_perimetre.py:356-361` : `main()` sans `try/except` ; `_lib.py:274-283` : exit 2 seulement sur message. Une exception donne exit 1, non bloquant pour un PreToolUse (constat écrit dans `test_gardes.py:511-513` pour le code 2 seulement). | Avant T4 : enveloppe fail-closed dans les trois `main()` (exception : `refuser("verrou en erreur")`) plus une sonde `doctor` qui le prouve. Chaque verrou neuf s'écrit entier dans le scratchpad, suite jouée dessus, puis une seule copie vers `.claude/hooks/`. Jamais d'édition en place d'un `garde_*.py`. |
| HAUTE | `plan-lot-3.md:65-69` (Défaire) | « Une tâche = un fichier, copie inverse » est faux après T7 : restaurer un seul `garde_*.py` de départ rappelle `_lib.decouper_commande` et consorts, retirés. Résultat : AttributeError, exit 1, verrou ouvert (finding 1). `test_gardes.py` est écrit par T6, T7 et T8 : sa copie inverse défait trois tâches. | grep : 3 usages dans `garde_git.py`, 5 dans `garde_perimetre.py`, 3 dans `garde_donnees.py`, 8 dans `test_gardes.py`. `verif_style.py`, `gate.py`, `nettoyer_caracteres.py` n'utilisent que `racine_projet`, `lire_entree`, `autoriser`, `refuser` : ÉCARTÉ pour eux. Copies actuelles identiques aux hooks (`cmp`, six fichiers). | Écrire : retour par fichier possible jusqu'à T6 ; à partir de T7, retour total des six copies puis `doctor --complet`. |
| HAUTE | `plan-lot-3.md:17` (T4) | « les 33 tests git en place verts » : compte intenable. | `grep def test_` : `GardeGitTests` 15, `GardeGitSousAgentTests` 4, soit 19. | Écrire « 19 tests des deux classes git » et compter à part les cas `git` du corpus. |
| MOYENNE | `plan-lot-3.md:18` (T5) contre `design-lot-3.md:153` (arbitrage 5) | « attendus inchangés » impossible : `python .claude/hooks/nettoyer_caracteres.py .env --appliquer` est REFUS en place ; en r2, script du dépôt sans indice d'écriture = LIT = PASSE. | `test_gardes.py:475` ; indices de `_programmes.py` (`plan:35`) sans `--appliquer`. | Trancher avant T3 : `nettoyer_caracteres.py` au catalogue en `SI_OPTION` (`--appliquer`), cas au corpus. |
| MOYENNE | `plan-lot-3.md:6` | « 98 tests, attendus inchangés » comme contrôle de fin de chaque tâche : faux dès T6 (test d'égalité ajouté), T7 (réécritures), T8 (315 cas importés). | Table T6 à T8. | « Les 98 de départ verts, ajouts et réécritures comptés au journal ». |
| MOYENNE | `plan-lot-3.md:15` contre `0011:57` | Deux seuils de performance (1 s contre 100 ms sur le corpus) et la promesse qui compte (`design:56-57`, un hook en quelques dizaines de ms, trois imports de plus) n'est mesurée nulle part. | Aucune mesure par appel dans le plan. | Un seul seuil ; `python garde_perimetre.py < entrée` chronométré à T0 et à T8, deux chiffres au journal. |
| MOYENNE | `design-lot-3.md:115-119`, `corpus_contrat.py:303-308` | Entrée fabriquée : `rm -f .git/index.lock`, réparation courante d'un git interrompu sous Windows, devient REFUS ; le corpus n'a que hooks, config, exclude. | Aucun cas `index.lock`. | Cas REFUS annoté au corpus avec le recours (main de Melvyn), phrase dans `securite.md` à T10. |
| BASSE | `plan-lot-3.md:21` contre `design:184` | Une sonde remplacée par deux donne huit sondes, la spec en promet sept. | `doctor.py:184-197` : sept cas. | Écrire « huit sondes ». |
| BASSE | `plan-lot-3.md:15` (T2) | « `VAR=x` retirées » sans dire « en tête seulement » : `dd if=/dev/zero of=.env` perdrait sa cible. | `test_gardes.py:468`. | Préciser dans la liste des tests T2. |

Dépendances : T3 dépend bien de T2 (type `Invocation`), T4 peut précéder T3, T7 puis T8 est cohérent ; rien de faux dans la table. `_programmes.py` déjà sur disque, conforme à l'arbitrage 7 de la décision.

Synthèse : l'ordre des tâches tient, mais le plan ignore que le poste travaille sans verrou à chaque exception d'un hook, et son retour arrière par fichier casse après T7 ; fail-closed et écriture atomique avant T4, sinon NEEDS_WORK.
